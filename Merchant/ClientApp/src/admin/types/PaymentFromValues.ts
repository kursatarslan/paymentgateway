// payment-form-values.interface.ts
export interface PaymentFormValues {
    firstName: string;
    lastName: string;
    email: string;
    creditCardNumber: string;
    amount: number
    // Add other fields as needed
}
